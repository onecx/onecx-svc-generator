entities:
  - name: Category
    aggregateRoot: false
    fields:
      - name:String

  - name: Product
    aggregateRoot: true
    fields:
      - name:String
      - price:BigDecimal
    relations:
      - category:ManyToOne:Category
